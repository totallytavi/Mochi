{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "App",
            "program": "${workspaceFolder}/index.js",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}